# Agents

This directory contains agent definitions — system prompts, role descriptions, and tool scopes.

Each agent is a role, not a model. The same underlying model can play multiple agents by loading different prompts and constraints.

See the architecture document for how agents are composed into councils.
